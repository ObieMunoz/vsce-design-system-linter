{
  "name": "token-recommendations",
  "displayName": "Token Recommendations",
  "description": "VS Code extension that provides token recommendations for hardcoded values.",
  "repository": {
    "type": "git",
    "url": "https://github.com/ObieMunoz/vsce-token-recommendations"
  },
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.63.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension.js",
  "contributes": {},
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.3.0",
    "@typescript-eslint/parser": "^5.3.0",
    "eslint": "^8.3.0",
    "typescript": "^5.0.4",
    "vscode": "^1.1.37"
  }
}
